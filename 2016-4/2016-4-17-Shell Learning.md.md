Shell
[web1](http://c.biancheng.net/cpp/shell/)

```sh
: '
notes
'
```
`H J K L`

`shift+a`

`0`

`yy yw y$ y^`

`5yy 2yw`

`:3，5y`

```sh
arr=(a b c)
len=${#arr[@]}
for i in `seq 0 $((len-1)); do
  echo "${i} ${arr[${i}]}
done
```
